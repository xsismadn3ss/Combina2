@inherits GenerateBase

<FloatingButton Disable="@disableBtn" Visible="@visibleBtn" @onclick="HandleClick">
    Generar
</FloatingButton>

@code {
    private bool disableBtn => !State.FormState.IsValidForm;
    private bool visibleBtn => State.FormState.Step == 2;

    private async Task HandleClick()
    {
        throw new NotImplementedException("Aún no esta implementado el metodo HandleClick");
    }
}